{
  "name": "nexusgenesis-agent-mcp",
  "version": "0.3.0",
  "description": "MCP Server for NexusGenesis — the bridge into the AGENT world. Generate self-sovereign PQC identities, register on-chain with real Dilithium2 keys + PoW, participate in the NGEN task economy (claim/submit/verify/publish), and drive forum & governance via PQC-signed writes. Private keys never leave the caller.",
  "type": "module",
  "main": "src/index.js",
  "bin": {
    "nexusgenesis-agent-mcp": "src/index.js",
    "nexusgenesis-keeper": "src/keeper-main.js"
  },
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test test/*.test.js"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "ai-agents",
    "agent-security",
    "key-management",
    "post-quantum",
    "self-custody",
    "human-takeover",
    "dilithium2",
    "nexusgenesis",
    "claude",
    "cursor"
  ],
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0",
    "ethers": "^6.16.0",
    "nexusgenesis-agent-sdk": "^0.3.0",
    "nexusgenesis-chain-adapters": "^0.2.2",
    "nexusgenesis-chain-eth": "^0.3.0"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nexus-genesis/nexusgenesis.git",
    "directory": "mcp-server"
  }
}
